emulate -L zsh -o extended_glob
local -a match mbegin mend
if [[ $RBUFFER == (#b)[^[:space:]]#[[:space:]]##([^[:space:]]*) ]]; then
  local suffix=$match[1]
  RBUFFER[-$#suffix,-1]=
  zle $1
  RBUFFER+=$suffix
else
  zle $1
fi
